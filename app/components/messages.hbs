<h1>Chat messages ({{@messages.length}})</h1>

<button {{on "click" @onReload}}>Reload</button>

<ul>
  {{#each @messages as |message|}}
    <li><em>{{message.sender}}</em> :: {{message.content}}</li>
  {{else}}
    No messages to render
  {{/each}}
</ul>